# Welcome note

A small markdown file that lives inside the pinned files root, so surfaces
have something real to read through `/api/files/read`.

- It renders as markdown if your surface chooses to.
- Its mtime changes if you edit it, which fires the change-signal stream.
