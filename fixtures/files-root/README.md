# files-root

This directory is the pinned root the runtime's read-only files verbs serve.
Everything under it is reachable through `/api/files/*`; nothing outside it is.

It is fixture content: safe demo files a surface can list, search, and read.
